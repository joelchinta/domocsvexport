name: Domo Export

on:
  repository_dispatch:
    types: [domo_export]
  workflow_dispatch:

env:
  PLAYWRIGHT_BROWSERS_PATH: 0

jobs:
  run-export:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      - name: Mask sensitive secrets in logs
        run: |
          echo "::add-mask::${{ secrets.DOMO_BASE_URL }}"
          echo "::add-mask::${{ secrets.DOMO_USERNAME }}"
          echo "::add-mask::${{ secrets.DOMO_PASSWORD }}"
          echo "::add-mask::${{ secrets.DOMO_VENDOR_ID }}"
          echo "::add-mask::${{ secrets.EMAIL_USER }}"
          echo "::add-mask::${{ secrets.EMAIL_TO }}"
          echo "::add-mask::${{ secrets.EMAIL_HOST }}"
          echo "::add-mask::${{ secrets.EMAIL_PORT }}"
          echo "::add-mask::${{ secrets.EMAIL_PASS }}"
          echo "::add-mask::${{ secrets.GOOGLE_CLIENT_ID }}"
          echo "::add-mask::${{ secrets.GOOGLE_CLIENT_SECRET }}"
          echo "::add-mask::${{ secrets.GOOGLE_REFRESH_TOKEN }}"
          echo "::add-mask::${{ secrets.EMAIL_SUBJECT }}"
          echo "::add-mask::${{ secrets.EMAIL_BODY }}"

      - run: npm start
        env:
          DOMO_BASE_URL: ${{ secrets.DOMO_BASE_URL }}
          DOMO_USERNAME: ${{ secrets.DOMO_USERNAME }}
          DOMO_PASSWORD: ${{ secrets.DOMO_PASSWORD }}
          DOMO_VENDOR_ID: ${{ secrets.DOMO_VENDOR_ID }}
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          EMAIL_HOST: ${{ secrets.EMAIL_HOST }}
          EMAIL_PORT: ${{ secrets.EMAIL_PORT }}
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
          EMAIL_SUBJECT: ${{ secrets.EMAIL_SUBJECT }}
          EMAIL_BODY: ${{ secrets.EMAIL_BODY }}
